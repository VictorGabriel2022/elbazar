import internal from "stream";

export interface DPedidoDTO{
    id_dped: integer,
    id_pedido: integer,
    id_prod: integer,
    cant: integer,
    precio: integer,
    subtotal: integer,
    estado: true,  
} 